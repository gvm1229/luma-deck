# Pragmata 2P Beta Presentation Local AI Directives

This deck inherits the `prag-init-presentation` visual language for the POTENUP beta presentation.

- Treat the visual base as a blank white canvas using `gray-100` as the primary color and `blue-600` as the accent color.
- Do not add grid, graph-paper, HUD, technical scanline, cyber/sci-fi gradient, orb, or grid-like background decorations.
- If scribbles are added later, keep them organic and hand-drawn, not grid-like or technical.
- Every title must be bold, including slide titles, section titles, card titles, description box titles, and image captions that function as titles.
- Prefer image-centric slides. Keep text-only slides rare and use them only when they satisfy a beta requirement more clearly than a visual slide.
- Never erase, replace, flatten, or reinterpret established deck design elements when refactoring content. In particular, preserve the first-slide background image, the last-slide background image, and the decorative background image applied across the interior slides.
- Screenshot placeholders apply only to foreground content images placed on top of the established slide backgrounds. A missing gameplay screenshot is not permission to remove or replace any background artwork, overlay, texture, or other inherited visual-system asset.
- The first slide is a cover page: use its established full-slide background image only. Do not add a foreground screenshot, screenshot placeholder, image frame, or secondary hero image to the cover.
- Keep the cover metadata tags on one horizontal line at the standard 16:9 presentation viewport. Do not constrain the cover-copy container to make room for a secondary image.
- For the beta deck, use actual in-game screenshots from the current beta build as the primary visual evidence. Do not use init-presentation asset imports, original-game reference shots, editor-only captures, or design graphs as substitutes for current gameplay evidence.
- When a suitable current-build screenshot is unavailable, keep a clearly labeled screenshot placeholder with an exact capture brief. The user will provide or capture the final image later.
- Before putting a numeric gameplay value on a slide, trace the effective runtime value across C++ defaults and current Blueprint class defaults. Project docs can lag behind Blueprint tuning and must not be the sole evidence for balance numbers.
- For technical implementation claims, trace the live C++ call path first and use project docs only as supporting context. Explicitly distinguish C++ defaults, current Blueprint CDO overrides, and runtime behavior.
- Describe the current UI architecture as mixed MVVM, not full-project MVVM. The Hugh and Diana root HUDs create separate local ViewModel instances over shared replicated gameplay sources; some POI, crosshair, enemy HP, and upgrade paths still use direct widget or delegate updates.
- Do not describe the root HUD ViewModel as converting server results into animation triggers. Present its verified FieldNotify outputs as HP, recovery, OverDrive, cleanse cooldown, player stats, and related display values. Hacking-puzzle trigger fields belong to separate puzzle ViewModels and should not be generalized to the role HUD.
- Describe GripGun as one server-side camera-viewpoint Line Trace. Do not claim a muzzle-origin trace or a second corrective trace unless the implementation changes.
- Describe Diana prop/node interaction as a camera-origin Visibility Line Trace-based surface test with a current C++ maximum trace default of `5000cm`; actual candidate distance is further limited by per-POI metadata. Local focus is advisory; the server re-runs the target search from its stored Diana camera origin/direction, checks the first blocking surface and POI metadata/distance, and only then executes the interaction. The boss-elevator floor is a narrow exception that ignores only the floor and re-traces for the actual PuzzleMesh. Map-hack nodes may auto-request while aimed, whereas ItemBox-style props still require the interaction input.
- Describe RoomCode as a LAN session search key, not authentication, security, or internet matchmaking.
- Explain that the Unreal Engine 5.8 upgrade also expanded the Editor API surface used by the project-owned `UnreelMCP` server; do not frame the upgrade as gameplay-only.
- Include the team's active AI usage as a technical workflow: Git change/commit automation, documentation organization and site generation, and human-reviewed Blueprint/Widget/MVVM semi-automation through UnreelMCP.
- Do not present AI automation as cost-free or fully autonomous. Pair it with the technical-debt response: Code Intent cards preserve the developer's reason, allowed/forbidden usage, call relationships, authority conditions, Blueprint links, evidence, inferred intent, uncertainty, validation, and future-change warnings.
- Use `images/slide-ai-code-intent.png` as the foreground evidence image for the Code Intent technical-debt slide. Preserve the established decorative slide background behind it.
- Static screenshots do not prove movement prediction by themselves. A movement-prediction slide must specify the same emulated network condition and use consecutive frames or a short live demonstration.
- Keep Korean wording professional and concise. Avoid casual phrases in visible slide text.
- After any slide visual/layout edit, including a single-slide edit, run a visual review test for the affected slide before claiming completion.
- Treat visible misalignment as a must-fix defect; aligned edges, consistent gaps, and stable baselines take priority over decorative variation.
- Connector arrows must render above every adjacent card, image, and decorative surface. `backdrop-filter` itself creates a stacking context, so removing `isolation` and raising only the arrow pseudo-element's `z-index` is insufficient. Preserve the translucent blur, give arrow-owning sibling cards reverse paint order with `:nth-last-child()` z-index values, and keep each arrow pseudo-element at the shared high z-index. Visually verify that the complete circular arrow, including the side overlapping the next card, remains visible.
- Do not represent short multi-step flows as full-height cards. For session or gameplay procedures, compress the sequence into a numbered horizontal route and reserve most of the slide for current-build visual evidence plus a small number of technical facts.
- Apply the same evidence-first composition across the deck without repeating one rigid template: use a compact numbered route for chronology, a large gameplay or project-record visual as the primary evidence, and no more than two compact conclusion cards. Preserve existing large-image technical slides when they already satisfy this hierarchy.
- When a technical slide is part of the main game-loop narrative, lead with the player-visible result and keep at most one short implementation proof in its caption or speaker notes. Avoid surfacing class names, RPC names, delegate names, exact geometry values, and internal state identifiers unless the detail is essential to the argument.
- Gameplay images must fill their image frame without unintended letterboxing or blank canvas. Use `object-fit: cover` for gameplay captures and match the frame to the source aspect ratio where possible; reserve `contain` only for diagrams, UI documents, or images where cropping would hide required evidence.
- Use `images/slide-07-notion-schedule.png` as the actual evidence image for beta slide 7. Its three workflow columns and task states must remain legible; keep the near-square evidence frame and do not replace it with a generic project-record placeholder.
- Use `images/slide-04-chaos-combat-hacking.png` as the actual evidence image for beta slide 4. Keep Hugh, the central enemy, and Diana's hacking puzzle visible in the widescreen frame; the slide-specific 19rem image height exists to closely match its 2.073:1 source ratio without letterboxing.
- Keep slide-specific layout instructions in `slides.md` and visual styling in `style.css` unless the user asks otherwise.
- Keep `uno.config.ts` dependency-free unless the repo adds a direct `unocss` dependency. Importing `defineConfig` from `unocss` can make `lumadeck dev` fail with `Cannot find module 'unocss'`.
- Keep `vite.config.ts` alias for `@fix-webm-duration/fix` unless Slidev or the dependency is upgraded. Slidev dev client can request a named `fixWebmDuration` ESM export from that CommonJS package and log `does not provide an export named 'fixWebmDuration'`.
- In this workspace, `pnpm lumadeck init` may try to run a pnpm install and abort in non-TTY mode. For this deck, copying an existing local authoring project and editing source files was the working path.
- This beta deck is a rehearsal base for the final presentation, but audience-facing copy must focus only on completed beta-build work and the beta demo.
- Keep member-specific honorable mentions in the final deck only; never copy these attribution slides back into the beta deck.
- 일반적인 증거 이미지는 정적 캡처를 우선한다. 윤제영 UI 시연 GIF 3개는 원본으로 보존하되 브라우저 합성 결함을 피하기 위해 H.264 MP4 재생본을 `LoopingMediaCanvas`로 그린다. 원작의 이동·사격·해킹 동시 수행을 보여 주는 `solo_do_all.gif`는 시간 흐름 자체가 설명 근거이므로 최종 덱에서 사용한다.
- 각 멤버 섹션은 `MEMBER SUMMARY · 이름` 슬라이드로 시작하고, 담당 범위·대표 성과·기술적 선택 또는 핵심 난제를 먼저 요약한다.
- 멤버의 성과와 트러블슈팅을 한 장에 합치지 않는다. 성과 하나와 트러블슈팅 하나는 각각 독립 슬라이드로 구성하고, 한 장에서는 하나의 주장만 설명한다.
- 최종 18분 발표에서는 모든 멤버를 `요약 1장 + 성과 1장 + 트러블슈팅 1장`으로 통일한다. 추가 사례는 별도 슬라이드를 늘리지 않고 관련 발표자 노트나 Q&A 대응에만 보완한다.
- 협업 방식·회고·검증 문서는 `TEAM OWNERSHIP`과 멤버별 성과보다 앞에 배치한다. 마지막 멤버 트러블슈팅 뒤에는 중복 결론 슬라이드를 덧붙이지 않고 Q&A로 바로 전환한다.
- 슬라이드 10 이후의 게임·레벨·HUD·문서 증거 이미지는 최종 빌드 기준 새 캡처가 제공될 때까지 구체적인 촬영 지시가 적힌 placeholder로 유지한다. 윤제영의 해킹 UI·맵 퍼즐 UI·무기 UI 영상은 이 규칙에서 제외하며 그대로 사용한다. 공통 장식 배경과 Q&A 배경도 교체 대상이 아니다.
- 멤버별 상세 성과에 들어가기 직전에는 다섯 명의 역할을 한 화면에서 비교하는 `TEAM OWNERSHIP` 슬라이드를 둔다. 각 `MEMBER SUMMARY · 이름` 슬라이드는 이미지 없이 동일한 3개 박스 구조로 역할·대표 결과·기술/협업 범위를 보여 주고, 바로 다음 슬라이드부터 해당 멤버의 성과와 트러블슈팅을 이어서 배치한다.
- Visible beta slide copy should use a professional Korean sentence-fragment / 음슴체 style such as `적용함`, `검증됨`, `진행 중`, and `가능`. Do not convert speaker notes to 음슴체.
- Audience-facing slide titles and labels must read as finalized presentation copy, not planning notes or instructions to the presenter. State the demonstrated game behavior, applied technology, achieved result, or explicit current boundary directly.
- Avoid meta-draft phrasing such as `설명함`, `보여줄 내용`, `명확히 밝힐 상태`, `판단함`, `정리 예정`, or `집중할 예정` in visible slide copy. Presentation framing such as `실제 게임과 적용 기술 시연을 중심으로 진행함` is acceptable when it describes the finalized agenda.
- Speaker notes must stay in polite `습니다` style because they are presenter script material, not audience-facing slide copy.
- Target the complete beta presentation at 18 minutes. Keep technical explanation through slide 19 near 12:05, reserve roughly 4:15 for the live beta demonstration on slide 20, and close with completion status, final-preparation priorities, and Q&A by 18:00. Timings belong only in speaker notes.
- The beta narrative is not a word-for-word formal TOC. It should follow the actual demo loop while still covering project overview, tech stack, collaboration/documentation, progress, features, demo, completion status, and next steps.
- The actual beta demo flow is: session connection and role selection -> cooperative exploration with Hugh movement and Diana hacking -> cooperative combat where Hugh deals proper damage after Diana hacks the enemy -> further exploration through varied devices -> tone shift from normal machines to tainted machines -> cleanse skill acquisition -> short exploration and large combat -> boss fight.
- Large combat and boss fight are not finished beta features. Present them as final-stabilization targets, not as complete beta accomplishments.
- This is a learning-purpose remake/fan project using assets from an existing commercial game. Do not imply commercial expansion, product roadmap, monetization, or original IP expansion potential.
- The project differentiation to emphasize is not "we copied Pragmata", but "we reinterpreted the original solo combat-plus-puzzle burden as two-player cooperation for fun."
- Listen Server may sound modest compared with Dedicated Server, but keep the technical learning points visible: Host/Join must be separated from Hugh/Diana role, role handoff must survive map travel, client input should be treated as intent, and server-owned state should drive hack target, map puzzle, combat state, and both HUDs.
- If using external CSS advice such as `css-pro-tips`, apply only conservative, baseline-aware improvements that preserve the existing white canvas + blue accent visual language. Prefer stable spacing, wrapping, alignment, image framing, and `@supports` guarded enhancements over flashy effects.
- Current beta screenshot evidence mapping is intentional and must remain foreground evidence: slides 2, 3, 5, 6, 8, 10, 13, 14, 15, and 16 use the corresponding `images/slide-*.png` assets added for the 2026-07-10 capture pass. Do not silently replace these with init-deck images.
- The former beta slide 19, `4개 이동 Intent를 네트워크 예측에 통합함`, remains outside the shared audience narrative. Movement prediction remains a technical implementation detail in the repository; preserve the final `18:00` endpoint when editing timing.
- Use `images/slide-18-ingame-01.png` for the `인게임 01` evidence slot on the map-interaction slide. Keep the second `인게임 02` slot available for a later gameplay capture.
- The final deck is synchronized from the latest beta deck for shared gameplay, technical, image, and timing content. Preserve the five attribution slides and their source images near the end; they are final-only content and must not be removed during future beta synchronization.
- Final presentation target remains 18:00. The live demo is shortened to `[10:50-15:20]` so five attribution sections and Q&A fit after the beta content.

## Final Presentation Evaluation Contract

- 최종 덱은 진행 보고가 아니라 100% 완성된 프로젝트의 기술 쇼케이스로 구성한다.
- 최종 평가 배점은 기술 완성도 30, 문제 해결 30, 협업 25, 문서·발표 10, 서비스 가능성 5를 기준으로 삼는다.
- 평가용 시연 영상은 5분 이내로 유지한다. 10분 전체 영상은 발표자 노트의 선택 자료로만 둔다.
- 원작을 모르는 관객을 위해 WHY와 두 역할을 먼저 설명한 뒤 시연으로 진입한다.
- 관객에게 `실제 캡처 필요`, `초안`, `미완성`, 향후 보강 같은 제작 메모를 노출하지 않는다. 실제 화면이 없으면 사실 기반의 일반 기술 다이어그램을 사용하되 실행 증거인 것처럼 꾸미지 않는다.
- Listen Server, Line Trace, Host/Remote, replication, null 같은 용어는 쉬운 한국어를 먼저 쓰고 필요한 경우 원어를 보충한다.
- 협업·일정·검증 주장은 실제 Git 기록과 `Docs`의 verification gate를 기준으로 작성한다. 최신 확인 기준은 2026-06-04 시작, 2026-07-12 최종 통합이다.
- 협업 도구는 Figma(화면 설계·상태 정렬), Notion(기획 의도·역할 분담·결정 맥락 공유), Code Intent/KNOW_HOW(구현 판단·권한 경계·검증 기록)로 구분해 설명한다. 도구 이름만 나열하지 않고 각 도구가 협업 흐름에서 맡은 역할을 함께 제시한다.
- 완료 검증은 Hugh/Diana의 서버 역할을 서로 바꾼 조합, 접속·맵 이동·역할 유지, 정화·후반 보스전 완주를 포함한다.
- 팀 기여 슬라이드는 T-pose나 임포트 화면보다 완성 플레이·기능 결과 이미지를 우선한다.
- 회고는 미완성 로드맵 대신 완성 과정에서 확보한 재사용 기반과 적용 가치를 설명한다.
- 모든 슬라이드는 1280×720에서 click 최종 상태까지 스크롤 오버플로 0건이어야 한다.
- 전체 덱에는 110MB GIF와 여러 MP4가 있어 Slidev CLI의 일괄 PDF export가 전환 중인 프레임을 검은 합성 블록으로 캡처할 수 있다. 최종 시각 판정은 1280×720 라이브 브라우저에서 전환이 끝난 뒤 수행하고, 검토용 캡처는 캐시 혼동을 피하도록 새 파일명을 사용한다. PDF가 필요하면 `--wait`를 사용하되 라이브 결과와 별도로 확인한다.
- 카드 깊이는 그림자가 아니라 테두리·색·여백으로 표현한다.
- 이미지 중심 슬라이드의 증거 영역은 제목 아래 가용 세로 공간을 적극 사용한다. `prag-focus-hero`, 트러블슈팅, 기여, 최종 검증 계열을 얕은 상단 띠로 축소해 하단 절반을 비우지 않는다.
- 이미지 면적을 키울 때는 기존 click 요소와 reveal 순서를 유지하고, 초기 cue에서 미래 요소가 `opacity: 0`과 `visibility: hidden`인지 다시 확인한다.
- 슬라이드 8과 9의 설명 박스는 이미지 위 별도 행에 둔다. 이미지와 박스를 겹치지 않으며, 슬라이드 8은 `images/slide-08-camera-aim-reference.png`를 사용한다.
- 슬라이드 8 일러스트는 카메라 중앙 조준점에서 첫 충돌 대상까지 이어지는 단일 시선 판정을 설명한다. muzzle-origin ray나 두 번째 보정 ray를 추가하지 않는다.
- 슬라이드 8 비교 이미지는 카메라 시선과 총구 방향의 시작점 차이를 설명하는 외부 참고 이미지다. 캡션에는 영상 제목 `눈에서 총알이 튀어나오는 FPS 게임의 사격 판정`을 하이퍼링크로 표시하고 실제 URL 문자열은 노출하지 않는다.
- `POTENUP 최종 프로젝트`는 교육 과정 소속을 나타내는 표지로 첫 장과 마지막 장에만 노출한다. 평가·채점 언어로 확장하지 않는다.
- 텍스트·도식 중심 슬라이드도 상단에 작은 카드만 두고 하단을 비우지 않는다. 3단 흐름은 핵심 노드를 충분히 크게 만들고 중앙 판정 단계를 강조하며, 3열 정보 카드는 제목 아래 가용 높이를 채운다.
- 시각 밀도를 높일 때 새 설명을 억지로 추가하지 않는다. 기존 핵심 문장을 단계·위계·크기로 재구성하고, 평가에 필요하지 않은 장식은 추가하지 않는다.
- 평가 배점과 검증 게이트 명칭은 내부 점검에만 사용한다. 관객 화면에는 `평가용`, `채점`, `Verification Gate`, `Final Clean Gate` 같은 표현을 노출하지 않고 작품·시연·최종 빌드의 언어로 표현한다.
- 슬라이드 4의 `시연 영상`은 전체 화면 레이어나 오버레이가 아니다. 네 관전 포인트 아래에 나타나는 일반 텍스트 cue로 유지한다.
- 제목을 제외한 본문이 짧은 도식·카드 묶음이면 제목 바로 아래에 붙이지 않고 남은 16:9 영역의 가로·세로 중앙에 배치한다. 슬라이드 4·6·11은 `prag-centered-body` 기준을 유지한다.
- 트러블슈팅 사례는 초보적인 null check보다 원격 애니메이션 시점, 조준 판정, AI Territory 경계, 비동기 stale callback, 대규모 Git history 정화처럼 담당 시스템의 판단·검증 과정이 드러나는 사례를 우선한다.
- 멤버 트러블슈팅은 관객 화면에 `문제 → 단순 해법이 실패한 제약 → 구조적 해결`만 크게 표시한다. 구체 타입·복제 조건·수명주기·검증 조합은 하단 `TECHNICAL CONTEXT`와 발표자 노트의 `[Q&A 대응]`에 분리해 기본 설명의 가독성과 심화 질문 대응을 함께 확보한다.
- 기본 발표 대본에는 내부 문서 파일명·경로·섹션명을 직접 말하지 않는다. 관객이 이해해야 할 판단과 결과를 일반 언어로 설명하고, 필요한 구현 식별자와 상세 호출 흐름만 `[Q&A 대응]`에 둔다.
- Figma, Notion, Code Intent는 하나의 협업 도구 묶음으로 합치지 않는다. 각각 독립 슬라이드에서 `화면 상태 합의`, `결정 맥락 공유`, `코드 변경 의도 보존`이라는 한 가지 역할만 설명한다.
- 프로젝트 수행 절차는 멘토 피드백의 `재미 우선` 기준, 예상 플레이 시나리오, 게임 아키텍처, 기술 선택 이유·구현 방법·기능 범위, 기술적 도전과 예외 대응이 앞부분에서 이어지게 구성한다. 상세 트러블슈팅은 담당자 귀속을 유지하되 앞부분의 대표 사례와 중복 설명하지 않는다.
- 멤버별 성과와 트러블슈팅 뒤에는 `기획 대비 완성도`, `팀이 잘한 점`, `팀의 아쉬운 점`, `다음 보완점`을 둔다. 수집되지 않은 팀원 한마디 placeholder는 관객용 덱에 넣지 않는다.
- 최종 회고 뒤에는 학습용 클론과 원작 에셋의 배포 한계를 숨기지 않고, 캐릭터·적·맵·애니메이션·재질 등 모든 추출 리소스를 자체 제작 또는 적법한 라이선스 리소스로 교체한 이후의 재사용 가능한 시스템 구조와 업데이트 방향을 별도 슬라이드로 설명한다.
- 관객 화면의 팀원 이름은 항상 풀네임을 사용한다. 발표자 노트에서는 발표자 정호진을 `저`로, 다른 팀원은 `준현님`, `지성님`, `민원님`, `제영님`처럼 이름 뒤 `님`으로 호칭한다.
- 다섯 명의 역할 분담 슬라이드는 한 줄 요약 대신 팀원별 책임을 3~4개 목록으로 나열해 한눈에 비교할 수 있게 한다.
- 멤버 요약 슬라이드의 세 카드는 한 문장만 두지 않고 책임·결과·기술 범위를 각각 3개 안팎의 목록으로 구성한다.
- 멤버별 성과와 트러블슈팅은 실제 최종 빌드 이미지 또는 기능을 직접 설명하는 도식을 우선한다. 아직 최종 캡처가 없으면 필요한 장면·상태·비교 구도를 구체적으로 적은 큰 placeholder를 두며, 윤제영 파트처럼 실제 영상이나 충분한 설명 도식이 이미 있는 슬라이드에는 중복 placeholder를 추가하지 않는다.
- 멤버별 섹션은 `역할 요약 1장 → 대표 성과 1장 → 트러블슈팅 1장`으로 통일한다. 한 멤버의 성과나 트러블슈팅을 여러 장으로 늘리지 않고, 세부 기술은 요약·발표자 노트·Q&A 대응으로 흡수한다.
- 5GB Git 에셋 이력 정화는 정호진 개인 성과나 개인 도전이 아니라 팀 전체 트러블슈팅으로 다룬다.
- 정호진의 대표 성과는 `UnreelMCP 개발 및 실전 적용`으로 고정한다. 대표 트러블슈팅은 Host 역할 조합에 따라 원격 Hugh의 달리기·조준·호버가 다르게 보이던 비대칭 복제 문제로 설명하며, 사망 연출은 시연하지 않으므로 대표 사례로 사용하지 않는다.
- 원격 Hugh 트러블슈팅은 `입력 의도 ≠ 서버 확정 상태 ≠ 원격 표현`의 경계를 핵심으로 설명한다. 입력은 SavedMove, 원격 의미 상태는 `FHughReplicatedAnimState`, 연속 이동량은 실제 `CharacterMovement` velocity/acceleration을 사용한다. 자동 테스트가 실제 Pose까지 검증했다고 과장하지 않는다.
- 안민원 맵 성과에는 랜드마크·길 안내·읽히는 진행 경로를 주장하지 않는다. 시작 구역에서 함정·플랫폼을 거쳐 보스전까지 흐름이 끊기지 않도록 공간을 이어 구성한 사실과 실제 4개 구간 이미지를 사용한다.
- Git 이력 정화 사례의 운영 수치는 사용자 제공 기록인 저장소 5GB 제한, 17,000여 대형 에셋, hojin 브랜치 76단계 rebase를 사용한다. 최종 통합일은 26.07.13이며 PR 번호는 관객 화면에 노출하지 않는다.
- Git 브랜치 전략과 커밋 메시지 규칙은 협업 방식으로, 5GB 제한을 넘긴 대형 에셋 history 정화는 트러블슈팅으로 구분한다. 두 내용을 같은 슬라이드에서 일반적인 병합 절차처럼 섞지 않는다.
- 협업 Git 슬라이드는 개인 작업 브랜치 → `develop` 통합 → `main` 안정본 흐름과 한 커밋 한 관심사, 한국어 Conventional Commits, 생성 파일 제외 규칙을 보여 준다.
- 기술 선택 슬라이드에서 Unreal Engine 5.8의 선택 이유는 확장된 Editor API를 활용한 UnreelMCP 기능 범위다. C++는 runtime 규칙·복제·검증, Blueprint/Editor는 레이아웃·에셋 배치·연출을 소유한다.
- AI와 UnreelMCP를 통한 빠른 Editor 자동화는 결과만 남을 때 설계 의도와 권한 경계가 사라지는 기술 부채를 만든다. Code Intent는 그 부채를 설계 이유·허용/금지 조건·검증 근거로 상환하는 흐름으로 설명한다.
- UnreelMCP 성과와 Code Intent를 한 장에 합치지 않는다. `slide-08-diana-title-animgraph.png`로 실제 AnimGraph 자동화 결과를 먼저 보여 준 뒤, 다음 슬라이드에서 그 자동화가 남길 수 있는 기술 부채를 Code Intent로 통제한 이유를 설명한다.
- 기능·성과 제목은 처음 보는 관객이 과거 버전을 알아야 이해되는 `전환`, `개선`, `기존 방식 대신` 같은 진행 보고 표현을 피한다. 현재 완성본이 무엇을 하고 플레이어에게 어떤 결과를 주는지 직접 말한다. 기술 상태 전환, 트러블슈팅의 전후 비교와 회고는 의미상 필요한 경우에만 유지한다.
- `PRAGMATA ©CAPCOM` 저작권 표기는 첫 표지와 마지막 Q&A 슬라이드에만 작게 노출한다. 본문 슬라이드에는 반복하지 않는다.
- 팀 기여와 트러블슈팅은 팀원별 담당 섹션에서 함께 설명한다. 독립된 트러블슈팅 묶음을 두지 않으며, 담당하지 않은 문제를 억지로 개인에게 귀속하지 않는다.
- 슬라이드 11은 `slide-05-walker-attack.png`와 `slide-11-tainted-executor-coop.png`를 나란히 사용한다. 두 이미지 프레임은 세로 공간을 충분히 확보하고 하단 공통 로직 설명은 압축해 전투 장면을 우선한다.
- 기술 스택·Git 이력 정화·팀원 카드에서 특정 박스만 정답처럼 파란 배경이나 강조 테두리로 표시하지 않는다.
- 마지막 Q&A 슬라이드는 init·beta와 동일하게 `layout: cover`, `class: prag-cover prag-close`를 사용해 `slide-48-qa-closing-background.jpg`를 유지한다.
- 최종 빌드 표기는 07.13으로 통일한다. 프로젝트 경계는 비상업적 학습 목적의 팬 프로젝트이며 원작 IP·추출 에셋의 권리를 주장하지 않는다.
- 체크포인트 업그레이드는 레벨 압박 흐름 뒤, 역할별 HUD 앞에서 `탐색 보상 → 체크포인트 선택 → 다음 전투 준비`를 설명한다. 실제 UI 이미지는 처음부터 크게 노출하고, 사용자가 확인하지 않은 강화 종류나 수치는 추가로 단정하지 않는다.
- 안지성의 대표 성과는 감지·추적·공격·순찰·Territory 복귀를 여러 적이 재사용하는 공통 Behavior Tree다. Territory 전투 흐름은 이 공통 규칙이 쓰이는 맥락으로 설명한다. 대표 트러블슈팅은 경계에서 BT가 반복 복귀하던 현상이며, Root Capsule 판정과 재진입 여유 거리로 상태 입력을 안정화한 사례로 설명한다.
- UnreelMCP는 정호진이 개발했지만 팀 전체의 Editor 자동화 도구로 설명한다. 정호진 개인 대표 성과는 Hugh의 점프·호버·이동 상태와 Diana의 추종·공중 애니메이션을 연결한 페어드 애니메이션이며, `slide-hojin-paired-animation-airborne.png`를 실제 증거 이미지로 사용한다.
- 박준현 섹션은 성과·트러블슈팅 모두 이미지 없이 텍스트 흐름으로 설명한다. 성과는 연속 이동 상태와 단발 행동을 분리해 조작 복귀까지 연결한 Hugh Player 설계, 트러블슈팅은 Damage Interface로 무기와 적의 직접 의존성을 끊은 구조다.
- 안민원 섹션은 성과 슬라이드의 실제 4개 맵 이미지로 충분하므로, 보스 엘리베이터 트러블슈팅에는 placeholder를 두지 않는다. 퍼즐 완료·이동 종료·전투 해제를 순서대로 수렴한 Stage 상태 흐름을 텍스트로 설명한다.
- 안지성 Territory 트러블슈팅도 placeholder를 두지 않는다. 경계에서 `bInsideTerritory`가 흔들려 BT가 반복 복귀한 문제를, 나갈 때와 재진입할 때의 조건을 다르게 둔 Re-entry Margin으로 안정화한 텍스트 흐름으로 설명한다.
- `TEAM RETROSPECTIVE · WHAT WENT WELL`은 일반적인 협업 회고가 아니라, 원작의 두 주인공을 2인 협동으로 확장한 출발 이유가 실제 상호의존 플레이 루프와 공동 성취감으로 달성됐다는 결론을 보여 준다.
- `TEAM RETROSPECTIVE · NEXT IMPROVEMENT`에서는 온보딩을 보완점으로 두지 않는다. LAN Listen Server만 검증한 범위를 원격 접속·이탈·재접속까지 넓히는 것, 일부 직접 Widget-게임 로직 연결이 남은 혼합 UI 구조를 ViewModel·FieldNotify 중심 MVVM으로 일관되게 정리하는 것을 실제 다음 보완점으로 설명한다.
